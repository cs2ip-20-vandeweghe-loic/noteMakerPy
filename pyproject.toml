[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "note"
version = "1.0.0"
description = "Simple CLI notes app"
requires-python = ">=3.8"

[project.scripts]
note = "note.cli:main"